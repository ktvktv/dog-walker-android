<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
<!--        <item name="colorAccent">@color/colorAccent</item>-->

        <item name="android:textViewStyle">@style/def</item>
        <item name="android:editTextStyle">@style/def</item>
        <item name="android:buttonStyle">@style/def</item>
        <item name="android:hint">@style/def</item>
    </style>

    <style name="def" parent="android:Widget">
        <item name="android:fontFamily">@font/raleway</item>
    </style>

</resources>
